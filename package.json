{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker Application with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon server.js",
    "client": "cd client && npm run dev",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": [
    "expense",
    "tracker",
    "react",
    "node",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
} 